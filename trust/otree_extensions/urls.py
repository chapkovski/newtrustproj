# from django.conf.urls import url, include
#
# # from trust.csv import *
# from trust.views import *
# views_to_add = [TestListView]
# urlpatterns = [url(i.url_pattern, i.as_view(), name=i.url_name) for i in views_to_add]
